#include <stdio.h>

void primero (int n1);
void segundo (int n2);
void iguales ();

int main(){
    int a, b;











    if (a > b) {
        primero(a);
    } else if (b > a) {
        segundo(b);
    } else {
        iguales();
    }
}